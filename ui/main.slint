import { AppWindow } from "app-window.slint";
import { WaveformImage } from "waveform-image.slint";

// Exportieren Sie die Hauptkomponente
export component MainApp inherits AppWindow {
    // Hier können Sie zusätzliche globale Eigenschaften oder Logik hinzufügen, falls erforderlich
}